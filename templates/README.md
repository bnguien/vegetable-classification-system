# Templates

This folder contains HTML templates used by the Flask web application.

## Contents
- `index.html`: Main interface for uploading images and displaying results

## Usage
Flask uses Jinja2 templating to render dynamic content.

## Notes
- You can extend this folder with more pages (e.g., result page, dashboard).